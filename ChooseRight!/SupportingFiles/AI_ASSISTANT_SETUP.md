# Настройка AI Ассистента с Groq API

AI Ассистент **уже настроен** на использование **Groq API** (быстрый и бесплатный LLM).

## ✅ Что уже сделано:

- ✅ Groq API активирован по умолчанию
- ✅ Метод `callGroqAPI()` готов к использованию
- ✅ Поле `GroqAPIKey` добавлено в `Info.plist`
- ✅ Код автоматически получает ключ из `Info.plist`

## 🚀 Осталось только одно:

### Добавьте ваш API ключ

**Вариант 1 (рекомендуется):** Через Info.plist
1. Откройте `Info.plist` в Xcode
2. Найдите ключ `GroqAPIKey`
3. Замените `YOUR_GROQ_API_KEY` на ваш реальный ключ из Groq

**Вариант 2:** Прямо в коде
1. Откройте `AIAssistantService.swift`
2. Найдите метод `callGroqAPI()` (строка ~472)
3. Замените `"YOUR_GROQ_API_KEY"` на ваш реальный ключ

### Как получить API ключ:

1. Зарегистрируйтесь на https://console.groq.com/
2. Перейдите в раздел **API Keys**
3. Нажмите **"Create API Key"**
4. Скопируйте созданный ключ
5. Вставьте его в `Info.plist` или код

## 🎉 Готово!

После добавления ключа AI ассистент будет работать с реальным LLM и генерировать значения сравнения автоматически.

---

## Дополнительная информация

### Преимущества Groq:
- ⚡ Очень быстрый ответ (< 1 секунда)
- 🆓 Бесплатный tier: 14,400 запросов/день
- ✅ Хорошее качество ответов
- 🔒 Безопасное хранение ключа в Info.plist

### Используемая модель:
- `llama-3.1-8b-instant` - быстрая модель (по умолчанию)
- Можно заменить на `mixtral-8x7b-32768` для лучшего качества (в методе `callGroqAPI()`)

### Как это работает:

1. Пользователь вводит запрос: "Хочу сравнить Москву и Дубай по уровню жизни, технологичности, стоимости"
2. Система парсит запрос и извлекает объекты и критерии
3. Создается строгий промпт для Groq API с инструкциями по JSON формату
4. Groq возвращает JSON с значениями "+" или "-"
5. JSON парсится и создается сравнение с заполненными данными

### Устранение проблем:

**Ошибка "networkError":**
- Проверьте, что API ключ правильный
- Убедитесь, что ключ добавлен в Info.plist
- Проверьте интернет-соединение

**Ошибка "invalidResponse":**
- Groq вернул невалидный JSON
- Система автоматически создаст сравнение с базовыми значениями
- Вы можете заполнить значения вручную

---

## Альтернативные провайдеры

Если хотите использовать другой API, найдите метод `callLLMAPI()` и замените:
```swift
return try await callGroqAPI(prompt: prompt)
```

На один из вариантов:
- `return try await callHuggingFaceAPI(prompt: prompt)`
- `return try await callOpenRouterAPI(prompt: prompt)`

Затем раскомментируйте соответствующий метод и добавьте API ключ.
